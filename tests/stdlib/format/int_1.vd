import std/core;
import std/string;
import std/format;

func main() {
	assert(format(4) == "4");
	assert(format(100) == "100");
	assert(format(123) == "123");
	assert(format(-321) == "-321");
	assert(format(-1000) == "-1000");
}