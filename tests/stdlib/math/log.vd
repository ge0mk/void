import std/core;
import std/math;

func main() -> void {
	assert(log!<f64>(1.0) == 0.0);
	assert(abs!<f64>(log!<f64>(2.0) - 0.69314) < 0.0001);
	assert(abs!<f64>(log!<f64>(3.0) - 1.09861) < 0.0001);
}
