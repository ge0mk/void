import std/core;
import std/math;

func main() -> void {
	const a = Matrix!<int, 3, 3>(
		1, 2, 3,
		4, 5, 6,
		7, 8, 9
	);

	const b = Vector!<int, 3>(1, 2, 3);
	assert(a * b == Vector!<int, 3>(14, 32, 50));
}
