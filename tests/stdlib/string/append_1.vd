//> output "hello world"

import std/core;
import std/string;
import std/io;

func main() -> void {
	var a = "hello";
	assert(a.size() == 5u);

	a.append(' 'b);
	assert(a.size() == 6u);

	a.append("world");
	assert(a.size() == 11u);

	assert(a == "hello world");
	println(a);
}
