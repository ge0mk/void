import std/core;
import std/string;

func main() -> void {
	const a = "hello world 123";
	const b = a.split(' 'b);
	assert(b.size() == 3u);
	assert(b[0u] == "hello");
	assert(b[1u] == "world");
	assert(b[2u] == "123");
}
