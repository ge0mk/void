import std/core;
import std/set;

func main() {
	var a = Set!<int>();
	a.add(2);
	a.add(1);
	a.add(0);

	assert(a.size() == 3);
	assert(a.contains(0));
	assert(a.contains(1));
	assert(a.contains(2));
}