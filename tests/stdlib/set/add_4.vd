import std/core;
import std/set;

func main() {
	var a = Set!<int>();
	assert(a.size() == 0);

	assert(a.getInsertPos(2) == 0);
	must a.add(2);

	assert(a.getInsertPos(1) == 0);
	must a.add(1);

	assert(a.getInsertPos(0) == 0);
	must a.add(0);

	assert(a.size() == 3);
}