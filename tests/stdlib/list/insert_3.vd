import std/core;
import std/list;

func main() {
	var a: List!<int>;
	must a.insert(0u, 2);
	must a.insert(0u, 1);
	must a.insert(0u, 0);

	assert((must a[0u]) == 0);
	assert((must a[1u]) == 1);
	assert((must a[2u]) == 2);
}