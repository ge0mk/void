import std/core;
import std/list;

func main() {
	var a = List!<int>();

	a.append(1);
	a.append(2);
	a.append(3);
	a.append(4);
	a.append(5);

	var b: List!<int> = a[1u..4u];
	assert(b.size() == 3u);
	assert(b[0u] == 2);
	assert(b[1u] == 3);
	assert(b[2u] == 4);
}
