import std/core;
import std/list;

func main() {
	var a: List!<int>;
	assert(a.size() == 0u);

	a.append(1);
	a.append(2);
	a.append(3);

	assert(a.size() == 3u);
	assert(a[0u] == 1);
	assert(a[1u] == 2);
	assert(a[2u] == 3);

	a.clear();
	assert(a.size() == 0u);
}
