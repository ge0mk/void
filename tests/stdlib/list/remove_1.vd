import std/core;
import std/list;

func main() {
	var a = List!<int>();

	must a.append(1);
	must a.append(2);
	must a.append(3);

	must a.remove(1u);
	assert(a.size() == 2u);

	assert((must a[0u]) == 1);
	assert((must a[1u]) == 3);
}
