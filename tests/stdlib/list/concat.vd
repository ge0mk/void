import std/core;
import std/list;

func main() {
	var a = List!<int>();
	var b = List!<int>();

	must a.append(1);
	must a.append(2);
	must a.append(3);

	must b.append(4);
	must b.append(5);
	must b.append(6);

	var c = a + b;

	assert(c.contains(2));
	assert(c.contains(5));
}
