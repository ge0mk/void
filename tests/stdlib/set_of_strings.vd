//> test-ec "0"

import std/core;
import std/set;
import std/string;

func main() {
	var a: Set!<String>;
	a.add("hello");
	a.add("world");

	assert(a.contains("hello"));
	assert(!a.contains("abc"));
}