//> test-arg "hello"
//> output "hello"
//> test-ec "0"

import std/core;
import std/range;
import std/string;
import std/io;

func main(args: Range!<String>) -> int {
	if var str = args[1u] {
		println(str);
		return 0;
	}

	return 1;
}
